void main(){
  List list = new List();
  list.add(12);
  print("The list has only one element/single  : ${list.single}");//check this list have one element or not if single
  // show the single value
  
}